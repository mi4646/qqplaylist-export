services:
  app:
    build: .
    ports:
      - "${PORT:-8081}:${PORT:-8081}"
    environment:
      - HOST=0.0.0.0
      - PORT=${PORT:-8081}
      - RATE_LIMIT=${RATE_LIMIT:-10/minute}
    restart: unless-stopped
